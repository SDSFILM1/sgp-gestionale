name: Mantieni attivo Supabase

on:
  schedule:
    - cron: '17 8 * * *'
  workflow_dispatch:

permissions: {}

jobs:
  keepalive:
    runs-on: ubuntu-latest
    steps:
      - name: Richiesta giornaliera senza dati
        run: >-
          curl --fail --silent --show-error --request POST
          'https://ogtyuhgmmdajqqkrakhi.supabase.co/rest/v1/rpc/sgp_keepalive'
          --header 'apikey: sb_publishable_xAO7ivpnTdebrHL6eurxEQ_bzBsJUZQ'
